// BUBBLE SORT
const bubbleSort = (array) => {
  for (i = 0; i < array.length - 1; i++) {
    for (j = i + 1; j < array.length; j++) {
      if (array[i] > array[j]) {
        [array[i], array[j]] = [array[j], array[i]];
      }
    }
  }
  return array
};
console.log(bubbleSort([10,2,3,5,31,1]));
